{
  "name": "jest-explore-config",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "yarn jest",
    "tl": "NODE_DEBUG=logging yarn jest",
    "tlb": "NODE_DEBUG=logging yarn jest --runInBand"
  },
  "dependencies": {
    "jest": "^26.3.0",
    "jest-circus": "^26.3.0"
  }
}